<div class="page-header">
      <h1>nous vous offrons les meilleures attractions</h1>
      <button class="btn">Preparer ma visite</button>
    </div>
    <div class="widget-container">
      <% @attractions.each do |attraction| %>
        <div class="widget">
          <div class="widget-content">
            <% if attraction.pictures.attached? %>
              <%= image_tag(attraction.pictures[0], :class => "widget-header", :style => 'width: 100px; margin-top: 20px') %>
            <% end %>
            <h3><%= attraction.name %></h3>
            <h5>Gain XP : <%= attraction.experience_points %> XP / partie</h5>
            <div><span class="subtitle">INTERDIT AUX MOINS D'1M10</span></div>
            <%= link_to attraction_path(attraction.id), :class => "btn" do %>
              voir l’attraction
            <% end %>
          </div>
        </div>
      <% end %>
    </div>
  </div>

<!-- 


<p id="notice"><%= notice %></p>

<h1>Attractions</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Experience points</th>
      <th>Maximum size</th>
      <th>Description</th>
      <th>Video presentation</th>
      <th>Image</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @attractions.each do |attraction| %>
      <tr>
        <td><%= attraction.name %></td>
        <td><%= attraction.experience_points %></td>
        <td><%= attraction.maximum_size %></td>
        <td><%= attraction.description %></td>
        <td><%= attraction.video_presentation %></td>
        <td>
          <% if attraction.pictures.attached? %>
            <%= image_tag(attraction.pictures[0], :style => "width: 100px") %>
            <%= image_tag(attraction.pictures[1], :style => "width: 100px") %>
          <% end %>
        </td>
        <td><%= link_to 'Show', attraction %></td>
      </tr>
    <% end %>
  </tbody>
</table>
-->