<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container">
    <%= link_to "GamZone", root_path, class: "navbar-brand" %> 
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
      <ul class="navbar-nav">
        <li class="nav-item">
          <div class="nav-link"><%= link_to "Le parc", park_path(Park.all.first) %> </div>
        </li>
        <li class="nav-item">
          <div class="nav-link">Contact</div>
        </li>
        
        <li class="nav-item">
          <div class="nav-link"><%= link_to "Ajouter une actualité", new_news_path %></div>
        </li>
      </ul>
      <ul class="navbar-nav right">
        <% if current_user %>

          <li class="nav-item">
            <div class="nav-link"><%= link_to "Se déconnecter", session_path(session.id), method: :delete, class: "text-danger" %> </div>
          </li>

        <% end %>

        <% if !current_user %>

          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
              Se connecter / S'inscrire
            </a>
            <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
              <div class="dropdown-item"><%= link_to "Se connecter", new_user_session_path %></div>
              <div class="dropdown-item"> <%= link_to "S'inscrire", new_user_registration_path %> </div>
            </div> 
          </li>

          <% end %>

      </ul>
    </div>
  </div>
</nav>